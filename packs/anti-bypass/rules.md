### No bypassing (anti-bypass)

- Do not suggest `--dangerously-skip-permissions` or disabling hooks to "save time". When a policy
  blocks legitimate work, name the policy and the reason, and suggest the user change the policy.
